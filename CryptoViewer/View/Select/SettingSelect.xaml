<UserControl
    x:Class="CryptoViewer.SettingSelect"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="0.5*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Column="0" Grid.Row="0" x:Uid="lang" VerticalAlignment="Center" />
    <ComboBox x:Name="selectLang" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch" Margin="4">
      <ComboBoxItem x:Uid="l_uk_UA" />
      <ComboBoxItem x:Uid="l_en_US" />
    </ComboBox>
    <TextBlock Grid.Column="0" Grid.Row="1" x:Uid="theme" VerticalAlignment="Center" />
    <ComboBox x:Name="selectTheme" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" Margin="4">
      <ComboBoxItem x:Uid="light" />
      <ComboBoxItem x:Uid="dark" />
    </ComboBox>
  </Grid>
</UserControl>