<UserControl
    x:Class="CryptoViewer.StarSelect"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid x:Name="Content" x:FieldModifier="public">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" x:Name="Title" x:Uid="starSubtitle" Margin="0 0 0 4" />
    <ScrollViewer x:Name="scrollViewer" Grid.Row="1" VerticalAlignment="Stretch">
      <StackPanel x:Name="scrollBody" VerticalAlignment="Stretch" Padding="0 0 20 0" />
    </ScrollViewer>
  </Grid>
</UserControl>